# ============================================================
# CallBot AutoAudit — Python Dependencies
# ============================================================

# Core
pydantic>=2.0.0
pyyaml>=6.0

# Async & Retry
tenacity>=8.2.0

# LLM & Embedding
openai>=1.30.0
anthropic>=0.39.0
google-genai>=0.3.0

# Vector DB
chromadb>=0.5.0

# Reranker
sentence-transformers>=3.0.0
torch>=2.0.0

# BM25 (Hybrid Search)
rank-bm25>=0.2.2
numpy>=1.24.0

# API Server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9        # 파일 업로드(UploadFile)

# KB 파일 업로드 파서 (고객사 지식 구축 — 다양한 포맷)
pypdf>=4.0.0                   # PDF
python-docx>=1.1.0            # Word(docx)
openpyxl>=3.1.0               # Excel(xlsx)

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Dev
python-dotenv>=1.0.0
